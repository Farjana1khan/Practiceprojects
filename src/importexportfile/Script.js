// Importing the module library containing // area and perimeter functions. //
//" ./ " is used if both the files are in the same folder.
const lib = require("./liabrary");
const length = 10;
const breadth = 5;
// Calling thefunctions;
// defined in the lib module
lib.area(length, breadth);
lib.perimeter(length, breadth);
